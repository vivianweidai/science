title: Vivian Dai
description: Mathematics Computing Physics Chemistry Biology Astronomy
baseurl: ""
plugins: []

# DEBUG: temporarily excluding research/ to see if the build succeeds
# without the 477MB tree. If yes, we know the problem is in there.
exclude:
  - apple/
  - .wrangler/
  - CLAUDE.md
  - MIGRATION.md
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor/
  - node_modules/
  - research/

defaults:
  - scope:
      path: "curriculum"
    values:
      render_with_liquid: false
